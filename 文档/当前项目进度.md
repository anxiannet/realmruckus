# 当前项目进度

更新时间：2026-07-14  
维护要求：每次完成项目任务后必须同步更新本文件

## 当前阶段

```text
新仓库已升级为通用规则主仓库
《夕妖：抢地盘》和Realm Ruckus均调整为具体表达
通用卡表、JSON数据、基础引擎、合法行动、回合状态机和效果框架已建立
当前进入剩余效果执行、完整回合执行器与AI策略阶段
```

## 已完成

### 主仓库与架构

- 已建立 `anxiannet/realmruckus` 通用规则主仓库
- 仓库根目录作为网站与代码根目录
- `文档/通用规则/` 为共享机制唯一来源
- 已明确《夕妖：抢地盘》和 Realm Ruckus 为具体表达

### 通用规则与数据

- 已建立通用核心机制、规则状态机和标准组件模型
- 已建立通用标准版卡表 `GEN-CORE-001`
- 已建立通用扩展包卡表 `GEN-EXP-001`
- 已建立双人6、9、12区域参数
- 已建立数学模型、测试规范和历史测试基线
- 已建立 `data/core_set.json`：40张、23个唯一机制卡面、12个区域槽位
- 已建立 `data/expansion_1.json`：10张、8个唯一机制卡面
- 合并公共牌库固定为50张
- 已建立卡表结构、数量、等级和区域分组校验

### 通用引擎

- 已建立数据模型、战斗结算、区域状态和卡表加载模块
- 已建立部署与进攻合法性判断
- 已建立合法部署目的地与合法进攻目标枚举
- 已建立标准回合阶段状态机
- 已限制每回合只能使用一次主要行动
- 已建立基于 `effect_id` 的效果注册和执行框架
- 已实现基础效果：抽牌、抽后弃、返回己方单位、按等级击败、按等级返回、多个单位返回、获得单位到手牌、强制弃牌、区域全体返回、区域低级单位清除、区域保护、社交回应抽牌、取消效果
- 效果分发不依赖具体卡名

### 测试与CI

- 已建立 `tests/test_engine.py`
- 已建立 `tests/test_data.py`
- 已建立 `tests/test_actions_turns_effects.py`
- 已覆盖同时伤害、累计伤害、双方全灭、战后恢复、区域回流、即时胜利、终局计分、卡表结构、合法行动、回合阶段和基础效果执行测试
- 已建立 `.github/workflows/generic-engine-tests.yml`
- 已更新 `simulation/README.md`，明确正式模拟尚未完成

### Realm Ruckus具体表达

- 品牌、世界观、英文规则、卡表、视觉、包装与视觉锚点文档已迁移
- 第一版视觉锚点已保存

## 当前待办

### P0：通用机制主仓库

1. 实现剩余效果ID：查看随机手牌、社交交换、弃牌堆回收、牌库顶选择、随机拿牌、暗选交换、替代弃牌、单位转移到区域、额外部署、立即进攻、社交获得单位、条件抽牌、支付弃牌费用。
2. 建立完整回合执行器，将抽牌、行动、效果、回流、胜利和手牌上限串联。
3. 建立自动目标选择接口。
4. 建立human_like策略。
5. 建立stress_attack策略。
6. 建立 `simulation.run` 批量运行入口。
7. 建立2、3、4人正式模拟工作流。
8. 使用新引擎复现历史3000局基线。
9. 建立两个具体表达的mechanic_id映射。

### P1：原表达迁移

1. 建立QDP与通用mechanic_id映射。
2. 建立Realm Ruckus与通用mechanic_id映射。
3. 清理具体表达中的平行核心规则。
4. 将原仓库历史模拟代码标记为归档来源。

### P2：Realm Ruckus资产

1. 英文名称与文案母语级复核。
2. 最终Logo与四个Realm徽章。
3. 卡框、卡背和指南卡UI。
4. 首批插画、卡面原型和印刷测试。

## 当前阻塞项

- 尚有13类效果未完成通用执行器。
- 完整回合执行器尚未完成。
- human_like与stress_attack策略尚未迁移。
- 历史3000局结果尚未由新引擎复现。
- 两个具体表达尚未完成mechanic_id映射。
- 正式商标、Logo、CMYK打样与包装法律信息尚未完成。

## 当前关键文件

```text
文档/通用规则/
data/core_set.json
data/expansion_1.json
engine/models.py
engine/combat.py
engine/state.py
engine/data.py
engine/actions.py
engine/turns.py
engine/effects.py
simulation/README.md
tests/
.github/workflows/generic-engine-tests.yml
```

## 进度更新规则

- 已完成事项从待办中移除并写入已完成。
- 不把框架建立写成全部效果完成。
- 不把结构校验写成正式平衡验证。
- 不把历史测试迁移写成新引擎验证通过。
- 进度必须以GitHub实际文件为依据。
- 不在其他文件维护第二份完整进度。
