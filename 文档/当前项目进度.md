# 当前项目进度

更新时间：2026-07-14  
维护要求：每次完成项目任务后必须同步更新本文件

## 当前阶段

```text
只迁移当前最新、可跨具体表达复用的通用内容
只做等价抽取、字段转换和来源核对
不新增规则、权重、标签、Schema、校准或引擎功能
所有改进与完整引擎开发留到迁移完成后的新项目阶段
```

## 当前迁移完成度

```text
约89%
```

## 已完成

### 通用规则与卡表

- 已迁移当前核心胜利条件、回合、部署、进攻、同时伤害、区域回流和最终结算。
- 已迁移当前双人快速、标准和完整长局参数。
- 已建立当前标准版和第一扩展包通用JSON。
- 当前有效组件基线为40张标准版、10张扩展包、50张合并牌库和12张区域牌。
- 已确认标准版唯一来源为 `V1.3-卡表-031`。
- 已确认项目记忆中的42+18状态不是当前卡表唯一来源，不进入迁移。
- 已核对标准版和扩展包的数量、等级、复制数与技能方向。
- 尚需继续核对少数单卡执行边界参数。

### 当前评分与AI

- 已完成技能强度评分文件的源表核对。
- 已删除源评分标准未直接给出的推导值。
- 当前评分文件只保留源标准中的锚点、基础分、战力修正、加扣分、牌区去向、行动节奏、信息技能和胜利相关性。
- 已迁移当前AI排序用途、二级排序、区域需求公式和社交回应概率。
- 已完成具体卡名到 `mechanic_id` 的逐项核对。
- 已将源 `card_priority()` 的31个原始数值等价迁移到：

```text
data/ai_priorities.json
```

- 已迁移当前战斗方案评分公式和执行阈值。
- 未新增情景标签、动态修正或新权重。

### 当前测试结果

- 已将当前双人快速与标准模式结果等价转换为：

```text
data/baselines/current_dual_modes.json
```

- 双人结果可追溯到 `规则/双人快速局可选规则.md`。
- `current_formal.json` 中2、3、4人结果仍需确认当前正式源报告，暂不判定完成。
- 不建立新增Schema或校准体系。

### 当前生产流程

- 已将通用生产流程改为当前源文件的等价迁移。
- 已恢复源规范中的后台关联字段：

```text
mechanic_id
skin_id
card_code
source_card_code
rules_effect_id
art_version
text_version
language
status
```

- 已恢复源规范中的具体表达独立目录结构。
- 已恢复源成品卡审核的四步顺序：视觉风格、技能匹配、留白空间、合成综合审核。
- 已恢复源返工边界和审核记录模板。
- 已删除自行设计的 `expression_id`、`layout_version`、自定义审核状态和 `expressions/` 目录结构。

### 来源追溯

已补齐：

- 当前规则入口路径和SHA；
- V1.3核心规则路径和SHA；
- V1.3标准版卡表路径和SHA；
- 第一扩展包卡表路径和SHA；
- 双人规则路径和SHA；
- 技能评分、AI排序、战斗评分和生产流程路径与SHA；
- 成品卡审核流程路径与SHA。

当前来源判定记录位于：

```text
文档/通用规则/迁移来源记录.md
```

### 已撤回的越界内容

- 已删除迁移阶段新增的基线Schema。
- 已删除自行设计的AI情景标签和动态修正。
- 已删除技能评分中的自行推导分值。
- 已删除生产流程中的自行设计字段和目录。
- 不再安排AI校准、战斗阈值优化、自动接口验证或新引擎开发。

## 当前待办

### P0：等价核对

1. 逐条核对核心规则剩余结算细则。
2. 逐效果核对标准版和扩展包JSON的执行边界参数。
3. 检查Markdown卡表与JSON同步。
4. 核对 `current_formal.json` 中的2、3、4人结果是否来自当前有效报告。

### P0：来源追溯

1. 找到当前40张和50张2、3、4人正式测试报告路径与SHA。
2. 若无法确认当前源报告，撤回或标记 `current_formal.json` 为待核对，不作为正式迁移完成项。
3. 确认所有通用文件均能直接追溯到当前源文件。

### P1：最终清理

1. 清理具体卡名、世界观、品牌卡号残留。
2. 清理历史版本和旧参数正文。
3. 清理迁移阶段产生的新增设计与工程规范。
4. 完成最终迁移完整性审核。

## 当前阻塞项

- 少数单卡执行边界参数尚未逐项核对。
- 当前2、3、4人正式测试结果的源报告尚未确认。
- 最终具体表达与历史内容残留检查尚未完成。
- 最终迁移审核尚未通过。

## 当前关键文件

```text
文档/通用规则/通用内容迁移范围.md
文档/通用规则/通用内容迁移清单.md
文档/通用规则/核心机制.md
文档/通用规则/通用标准版卡表.md
文档/通用规则/通用扩展包卡表.md
文档/通用规则/技能强度评分系统.md
文档/通用规则/AI卡牌优先级模型.md
文档/通用规则/战斗方案评分模型.md
文档/通用规则/通用生产与审核流程.md
文档/通用规则/迁移来源记录.md
data/core_set.json
data/expansion_1.json
data/ai_priorities.json
data/baselines/current_formal.json
data/baselines/current_dual_modes.json
```

## 进度更新规则

- 只计算等价迁移完成度。
- 新设计、新数据、新校准和新代码不计入迁移。
- 无法直接追溯到当前源文件的内容必须撤回或标记待核对。
- 迁移审核通过前不进行改进和完整引擎开发。
- 不在其他文件维护第二份完整进度。
