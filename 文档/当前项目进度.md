# 当前项目进度

更新时间：2026-07-14  
维护要求：每次完成项目任务后必须同步更新本文件

## 当前阶段

```text
新仓库已升级为通用规则主仓库
《夕妖：抢地盘》和Realm Ruckus均调整为具体表达
JSON唯一来源、统一卡牌实例、完整规则执行骨架和全部效果编排已建立
当前进入目标选择、AI策略、批量模拟和历史测试复现阶段
```

## 已完成

### 主仓库与架构

- 已建立 `anxiannet/realmruckus` 通用规则主仓库
- 仓库根目录作为网站与代码根目录
- `文档/通用规则/` 为共享机制文档层
- 已明确《夕妖：抢地盘》和 Realm Ruckus 为具体表达
- 已完成迁移完整性审核：`文档/迁移完整性审核-2026-07-14.md`
- 当前综合迁移完整度评估约80%

### 数据唯一来源与版本基线

- 已确立 `data/core_set.json` 为标准版机制数据唯一来源
- 已确立 `data/expansion_1.json` 为第一扩展包机制数据唯一来源
- 已建立 `文档/通用规则/数据唯一来源规范.md`
- 已将两份Markdown卡表调整为JSON派生说明文档
- 已建立 `文档/通用规则/版本基线裁决记录.md`
- 已正式裁决当前基线为40张标准版、10张扩展包、50张合并牌库
- 已将42张与60张方案标记为历史候选

### 通用规则与数据

- 已建立通用核心机制、规则状态机和标准组件模型
- 已建立通用标准版卡表 `GEN-CORE-001`
- 已建立通用扩展包卡表 `GEN-EXP-001`
- 已建立双人6、9、12区域参数
- 已建立数学模型、测试规范和历史测试基线
- 标准版JSON：40张、23个唯一机制卡面、12个区域槽位
- 扩展JSON：10张、8个唯一机制卡面
- 已建立卡表结构、数量、等级、唯一ID和区域分组校验

### 通用引擎

- 已建立统一 `CardInstance`
- 已建立 `Unit` 与 `EffectCard` 两类卡牌实例
- 已建立卡牌UID和拥有者字段
- 已建立 `engine/factory.py`，可从JSON定义生成实际卡牌实例
- 已建立基础战斗、区域状态、卡表加载、合法行动和回合阶段模块
- 已迁移战斗单位顺序与防守目标组合枚举
- 已建立基于 `effect_id` 的效果注册与执行框架
- `EffectContext` 已全面切换为统一 `CardInstance` 手牌、牌库和弃牌堆
- 已实现所有可独立执行的标准版与扩展包效果
- 已建立 `engine/game.py` 通用完整回合执行器骨架
- 已串联抽牌、行动、效果、区域回流、即时胜利、手牌上限、空库整轮判断与下一玩家
- 已实现替代击败：指定机制被击败后改为洗回公共牌库
- 已实现额外部署，并使用普通部署和入场效果流程
- 已实现立即进攻，并使用普通进攻与战斗流程
- 已建立取消效果基础响应窗口
- 已建立区域保护持续时间，并在保护所属玩家下回合开始时清除
- 已建立即时胜利和空库最终比较的 `GameResult`
- 效果分发与规则执行不依赖具体卡名

### 测试与CI

- 已建立基础引擎、数据、行动、回合、效果、卡牌实例和战斗方案测试
- 已新增 `tests/test_extended_effects.py`
- 已新增 `tests/test_game_engine.py`
- 已覆盖保护清除、替代击败、额外部署、立即进攻、取消响应、正常效果结算、即时胜利和空库最终结算
- JSON中的全部效果已由独立执行器或 `GameEngine` 编排处理
- 已建立 `.github/workflows/generic-engine-tests.yml`
- 已迁移历史测试汇总，但未迁移原始JSON结果

### Realm Ruckus具体表达

- 品牌、世界观、英文规则、卡表、视觉、包装与视觉锚点文档已迁移
- 第一版视觉锚点已保存

## 当前待办

### P0：目标选择与自动对局

1. 建立统一合法目标枚举与效果上下文生成器。
2. 建立部署、进攻、效果牌和社交回应的自动动作候选。
3. 完善费用和非法目标的行动前验证，避免先付费后发现效果非法。
4. 将基础取消响应扩展为明确响应链状态。
5. 建立完整对局事件日志与逐卡统计。

### P0：AI与测试复现

1. 迁移human_like策略。
2. 迁移stress_attack策略。
3. 建立 `simulation.run` 批量运行入口。
4. 建立2、3、4人自动对局烟雾测试。
5. 迁移旧的原始测试JSON和逐卡统计。
6. 建立100局回归与每组3000局正式测试工作流。
7. 建立历史基线自动比较与容差规则。
8. 在合理容差内复现历史结果。
9. 取得明确CI成功记录。

### P1：唯一来源与具体表达收束

1. 修正Realm Ruckus专属文件中的旧路径和旧权威声明。
2. 将原仓库旧规则和模拟器标记为历史来源或具体表达文件。
3. 建立QDP与通用mechanic_id映射。
4. 建立Realm Ruckus与通用mechanic_id映射。
5. 清理两个表达中的平行核心规则。
6. 建立从JSON生成Markdown卡表的工具。
7. 建立通用表达、卡号、版本和生产审核流程。

### P2：Realm Ruckus资产

1. 英文名称与文案母语级复核。
2. 最终Logo与四个Realm徽章。
3. 卡框、卡背和指南卡UI。
4. 首批插画、卡面原型和印刷测试。

## 当前阻塞项

- `GameEngine` 仍依赖外部明确提供动作、效果目标和部分效果上下文，尚不能自行完成自动对局。
- 当前响应窗口只支持单次取消，不支持多层响应链。
- human_like与stress_attack策略尚未迁移。
- 批量运行入口和自动对局尚未建立。
- 原始测试JSON和逐卡统计尚未迁移。
- 历史3000局结果尚未由新引擎复现。
- QDP和RRK均未完成mechanic_id映射。
- 原仓库与Realm Ruckus文件仍存在旧权威声明和失效路径。
- Markdown生成工具尚未建立，目前仍需人工同步派生说明。
- CI在线成功状态尚未确认。
- 正式商标、Logo、CMYK打样与包装法律信息尚未完成。

## 当前关键文件

```text
文档/迁移完整性审核-2026-07-14.md
文档/通用规则/数据唯一来源规范.md
文档/通用规则/版本基线裁决记录.md
data/core_set.json
data/expansion_1.json
engine/models.py
engine/factory.py
engine/combat.py
engine/effects.py
engine/game.py
simulation/README.md
tests/test_game_engine.py
.github/workflows/generic-engine-tests.yml
```

## 进度更新规则

- 已完成事项从待办中移除并写入已完成。
- 不把外部提供动作的规则执行器写成自动对局模拟器。
- 不把单次取消响应写成完整响应链。
- 不把单元测试存在写成CI在线通过。
- 不把结构校验写成正式平衡验证。
- 不把历史测试汇总迁移写成新引擎验证通过。
- 进度必须以GitHub实际文件为依据。
- 不在其他文件维护第二份完整进度。
