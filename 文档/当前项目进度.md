# 当前项目进度

更新时间：2026-07-14  
维护要求：每次完成项目任务后必须同步更新本文件

## 当前阶段

```text
只迁移可跨具体表达复用的通用内容
不迁移具体卡名、角色、世界观、卡号、文案、插画、包装和品牌UI
通用规则、通用卡表和三套评分系统的主体文件已建立
当前重点是AI优先级数据、测试基线JSON、通用流程和来源追溯
迁移审核通过后，再继续完整通用引擎开发
```

## 当前迁移完成度

```text
约72%
```

详细审核：

```text
文档/通用内容迁移完整性审核-2026-07-14.md
```

逐项清单：

```text
文档/通用规则/通用内容迁移清单.md
```

## 已完成

### 迁移边界

- 已建立 `文档/通用规则/通用内容迁移范围.md`
- 已明确只抽取通用机制、公式、流程、参数和测试方法
- 已删除“原样归档具体表达”的迁移方案
- 候选引擎代码不计入迁移完成度
- 已清理通用规则README中的具体产品名称

### 通用核心规则

- 已建立通用核心机制
- 已建立规则状态机
- 已建立标准组件模型
- 已建立双人6、9、12区域参数
- 已抽取部署、进攻、同时伤害、区域回流、即时胜利和牌库耗尽结算

### 通用卡表

- 已确立 `data/core_set.json` 为标准版唯一机制数据源
- 已确立 `data/expansion_1.json` 为第一扩展包唯一机制数据源
- 已建立 `mechanic_id`、`effect_id`、等级、数量、时机和参数
- 已确定40张标准版、10张扩展包、50张合并牌库基线
- 已将42张与60张方案标记为历史候选

### 数学与测试框架

- 已建立战斗基础数学
- 已建立区域胜利和牌库压力模型
- 已建立平均回合、中位回合、P90、胜利率、结算率、超时率和牌库耗尽率指标
- 已建立成功进攻、失败进攻和区域回流指标
- 已建立随机种子和版本记录要求
- 已明确human_like与stress_attack的用途边界
- 已迁移通用参数形式的历史测试汇总

### 技能强度评分系统

- 已建立 `文档/通用规则/技能强度评分系统.md`
- 已迁移无上限累计整数分和锚点优先原则
- 已将具体名称锚点转换为抽象机制锚点
- 已迁移受影响战力、数量、持续时间、区域连锁、行动节奏、资源差和自由度修正
- 已迁移等级限制、前置条件、成本和反制扣分
- 已建立强度区间和统一输出格式
- 已记录原路径、SHA、抽取范围和排除项

### AI卡牌优先级模型

- 已建立 `文档/通用规则/AI卡牌优先级模型.md`
- 已迁移等级与优先级二级排序
- 已迁移弃最低、选最高的手牌和公开候选牌逻辑
- 已迁移区域需求公式
- 已迁移部署前判断和社交回应概率
- 已移除具体卡名权重表
- 尚未为每个 `mechanic_id` 分配正式 `ai_base_priority`

### 战斗方案评分模型

- 已建立 `文档/通用规则/战斗方案评分模型.md`
- 已迁移单位交换价值公式
- 已迁移成功夺区、同组胜利进度和阻止对手胜利奖励
- 已迁移失败、无效交换、双方全灭和清空出兵区惩罚
- 已迁移human_like、pressure和forced_attack阈值
- 已分离历史参数和当前收紧参数
- 已记录原路径、SHA、抽取范围和排除项

## 当前待办

### P0：AI优先级数据

1. 为通用标准版和扩展包的每个 `mechanic_id` 建立 `ai_base_priority`。
2. 建立完整情景标签与权重。
3. 检查AI优先级与技能强度评分不存在直接等同关系。
4. 使用历史模拟数据校准优先级。

### P0：通用测试基线

1. 将历史测试汇总转换为 `data/baselines/*.json`。
2. 记录源结果路径、版本和参数。
3. 移除具体名称统计。
4. 逐机制统计统一改为 `mechanic_id` 或 `effect_id`。
5. 建立基线字段校验。

### P1：通用流程与来源追溯

1. 抽取通用表达映射流程。
2. 抽取通用卡号与版本流程。
3. 抽取通用卡面、插画、印刷、返工和归档流程。
4. 为每个通用文件补充原路径、SHA、相关提交、抽取范围和排除项。
5. 继续清理其他通用文件中的具体表达残留。

### P1：评分系统验证

1. 使用通用 `effect_id` 建立技能强度评分示例集。
2. 对标准版和扩展包进行独立技能评分审核。
3. 验证战斗评分与通用数据接口。
4. 使用通用基线校准AI与战斗阈值。

### P2：最终审核

1. 逐条核对全部源规则细则。
2. 逐效果核对标准版和扩展包JSON。
3. 检查通用层具体表达残留。
4. 完成最终通用内容迁移审核。
5. 审核通过后恢复完整通用引擎开发。

## 当前阻塞项

- 每个 `mechanic_id` 的正式AI基础优先级尚未建立。
- 历史测试基线尚未转换为机器可读JSON。
- 通用生产与审核流程尚未完整抽取。
- 来源追溯尚未逐文件完成。
- 标准版与扩展包尚未完成技能评分审核。
- AI和战斗阈值尚未通过通用基线校准。
- 最终迁移审核尚未通过。

## 当前关键文件

```text
文档/通用规则/通用内容迁移范围.md
文档/通用规则/通用内容迁移清单.md
文档/通用内容迁移完整性审核-2026-07-14.md
文档/通用规则/核心机制.md
文档/通用规则/数学与平衡模型.md
文档/通用规则/技能强度评分系统.md
文档/通用规则/AI卡牌优先级模型.md
文档/通用规则/战斗方案评分模型.md
data/core_set.json
data/expansion_1.json
文档/当前项目进度.md
```

## 进度更新规则

- 只计算通用内容迁移完成度。
- 不把具体表达文件计入完成项。
- 不把候选引擎代码数量计入迁移完成度。
- 已盘点不等于已迁移。
- 已迁移不等于已完成来源追溯。
- 三套评分系统必须分别维护。
- 评分文件存在不等于数据校准完成。
- 迁移审核通过前不继续完整引擎开发。
- 不在其他文件维护第二份完整进度。
