# 当前项目进度

更新时间：2026-07-14  
维护要求：每次完成项目任务后必须同步更新本文件

## 当前阶段

```text
只迁移当前最新、可跨具体表达复用的通用内容
不迁移历史版本、旧参数、历史候选或旧测试结果
不迁移具体卡名、角色、世界观、卡号、文案、插画、包装和品牌UI
通用规则、通用卡表和三套评分系统主体已建立
当前重点是AI优先级数据、当前测试基线JSON、通用流程和来源追溯
迁移审核通过后，再继续完整通用引擎开发
```

## 当前迁移完成度

```text
约75%
```

逐项清单：

```text
文档/通用规则/通用内容迁移清单.md
```

## 已完成

### 迁移边界

- 已明确只保留当前最新有效内容
- 已删除“原样归档具体表达”的迁移方案
- 已删除历史测试基线文件
- 已将版本文件改为 `当前版本基线.md`
- 已建立 `当前测试基线.md`
- 候选引擎代码不计入迁移完成度

### 通用核心规则

- 已建立通用核心机制
- 已建立规则状态机
- 已建立标准组件模型
- 已建立双人快速、标准和完整长局参数
- 已抽取部署、进攻、同时伤害、区域回流、即时胜利和牌库耗尽结算

### 通用卡表

- 已确立 `data/core_set.json` 为标准版唯一机制数据源
- 已确立 `data/expansion_1.json` 为第一扩展包唯一机制数据源
- 已建立 `mechanic_id`、`effect_id`、等级、数量、时机和参数
- 当前组件基线为40张标准版、10张扩展包、50张合并牌库、12张区域牌

### 当前版本与测试基线

- 已建立 `文档/通用规则/当前版本基线.md`
- 已建立 `文档/通用规则/当前测试基线.md`
- 当前双人模式：6区域快速、9区域标准默认、12区域完整长局
- 当前公开正式样本总量为30000局
- 已保留当前2、3、4人正式结果和双人区域参数结果
- 已移除旧组件数量、旧AI参数和历史候选测试正文

### 技能强度评分系统

- 已建立 `文档/通用规则/技能强度评分系统.md`
- 已迁移当前无上限累计整数分和锚点优先原则
- 已建立抽象机制锚点
- 已迁移当前战力、数量、持续时间、区域连锁、行动节奏、资源差和限制修正
- 已建立当前强度区间和输出格式

### AI卡牌优先级模型

- 已建立 `文档/通用规则/AI卡牌优先级模型.md`
- 已迁移当前等级与优先级二级排序
- 已迁移当前弃牌、选牌、区域需求、部署倾向和社交回应规则
- 已移除具体卡名权重表
- 尚未为每个 `mechanic_id` 分配正式 `ai_base_priority`

### 战斗方案评分模型

- 已建立 `文档/通用规则/战斗方案评分模型.md`
- 已只保留当前评分公式和当前阈值
- 已移除被当前版本替代的历史参数
- 当前阈值：pressure 8、human_like 14、forced_attack -999

## 当前待办

### P0：AI优先级数据

1. 为当前标准版和扩展包的每个 `mechanic_id` 建立 `ai_base_priority`。
2. 建立当前情景标签与权重。
3. 检查AI优先级与技能强度评分不存在直接等同关系。
4. 使用当前正式测试结果校准优先级。

### P0：当前测试基线机器化

1. 将当前测试结果转换为 `data/baselines/*.json`。
2. 只保留当前规则、卡表和AI模型参数。
3. 逐机制统计统一使用 `mechanic_id` 或 `effect_id`。
4. 建立当前基线字段校验。

### P1：通用流程与来源追溯

1. 抽取当前通用表达映射流程。
2. 抽取当前通用卡号与版本流程。
3. 抽取当前通用卡面、插画、印刷、返工和归档流程。
4. 为每个通用文件补充当前源路径、SHA、抽取范围和排除项。
5. 清理通用层中的历史版本正文和具体表达残留。

### P1：评分系统验证

1. 使用通用 `effect_id` 建立当前评分示例集。
2. 对当前标准版和扩展包进行技能评分审核。
3. 验证战斗评分与通用数据接口。
4. 使用当前测试基线校准AI和战斗阈值。

### P2：最终审核

1. 逐条核对当前源规则细则。
2. 逐效果核对当前标准版和扩展包JSON。
3. 检查通用层具体表达残留。
4. 检查通用层历史版本和旧参数残留。
5. 完成最终通用内容迁移审核。
6. 审核通过后恢复完整通用引擎开发。

## 当前阻塞项

- 每个 `mechanic_id` 的正式AI基础优先级尚未建立。
- 当前测试结果尚未转换为机器可读JSON。
- 通用生产与审核流程尚未完整抽取。
- 来源追溯尚未逐文件完成。
- 当前标准版与扩展包尚未完成技能评分审核。
- AI和战斗阈值尚未通过当前测试基线校准。
- 最终迁移审核尚未通过。

## 当前关键文件

```text
文档/通用规则/通用内容迁移范围.md
文档/通用规则/通用内容迁移清单.md
文档/通用规则/当前版本基线.md
文档/通用规则/当前测试基线.md
文档/通用规则/核心机制.md
文档/通用规则/数学与平衡模型.md
文档/通用规则/技能强度评分系统.md
文档/通用规则/AI卡牌优先级模型.md
文档/通用规则/战斗方案评分模型.md
data/core_set.json
data/expansion_1.json
文档/当前项目进度.md
```

## 进度更新规则

- 只计算当前最新通用内容迁移完成度。
- 不把历史版本、旧参数或旧测试结果计入迁移范围。
- 不把具体表达文件计入完成项。
- 不把候选引擎代码数量计入迁移完成度。
- 三套评分系统必须分别维护。
- 评分文件存在不等于数据校准完成。
- 迁移审核通过前不继续完整引擎开发。
- 不在其他文件维护第二份完整进度。
